import { StyleSheet } from "react-native";
const StarRatingStyle = StyleSheet.create({
  view: {
    flexDirection: "row",
    alignItems: "center",
    backgroundColor: "transparent",
    width: 110,
    justifyContent: "space-between",
    paddingVertical: 5,
  },
});
export default StarRatingStyle;
