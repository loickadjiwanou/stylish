import { View, Text } from "react-native";
import CardStyle from "./Card.style.js";
const Card = (props) => {
  return (
    <View style={CardStyle}>
      <Text>Card</Text>
    </View>
  );
};
export default Card;
